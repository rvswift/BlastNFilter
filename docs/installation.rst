============
Installation
============

At the command line::

    $ easy_install BlastNFilter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv BlastNFilter
    $ pip install BlastNFilter
